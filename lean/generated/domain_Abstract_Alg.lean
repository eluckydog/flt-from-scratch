/-
  Domain concept index
-/

Mathlib
import Mathlib.Data.ZMod.Basic
import Mathlib.Algebra.GroupPower.Basic
import Mathlib.Algebra.FieldPower
import Mathlib.RingTheory.Polynomial
import Mathlib.FieldTheory.Galois

namespace FLT.Abstract_Algebra

/* 2脳2_matrices_mat鈧俽 鈥?Square matrices over commutative ring R. (phase 1, importance 3) */
structure n2脳2_matrices_mat鈧俽 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* abelian 鈥? (phase 1, importance 3) */
structure abelian where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* adele 鈥? (phase 1, importance medium) */
structure adele where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* algebraic_closure_q虅 鈥?Smallest extension where all polys split. Galois over Q. (phase 1, importance 5) */
structure algebraic_closure_q虅 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* algebraic_integer 鈥? (phase 1, importance 3) */
structure algebraic_integer where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* algebraic_norm 鈥? (phase 1, importance 3) */
structure algebraic_norm where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* associate 鈥? (phase 1, importance 3) */
structure associate where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* autk_f 鈥?Field automorphisms of K fixing F. (phase 1, importance 4) */
structure autk_f where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* characteristic_polynomial 鈥?位虏-tr(T)位+det(T) for 2-dim. (phase 1, importance 3) */
structure characteristic_polynomial where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* coprime 鈥? (phase 1, importance 3) */
theorem coprime_exists_in_mathlib4 : True := by
  trivial
  -- Type: Nat.Coprime

/* cyclotomic 鈥? (phase 1, importance 3) */
structure cyclotomic where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* cyclotomic_field 鈥? (phase 1, importance 3) */
structure cyclotomic_field where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* determinant 鈥? (phase 1, importance 3) */
structure determinant where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* eigenvalues_and_eigenvectors 鈥?Tv=位v. Trace=sum, determinant=product of eigenvalues. (phase 1, importance 3) */
structure eigenvalues_and_eigenvectors where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* eisenstein_integer 鈥? (phase 1, importance 3) */
structure eisenstein_integer where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* extension 鈥? (phase 1, importance 3) */
structure extension where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* field 鈥? (phase 1, importance 3) */
theorem field_exists_in_mathlib4 : True := by
  trivial
  -- Type: Field

/* field_extension_k_f 鈥?K contains F as subfield. K is F-vector space. (phase 1, importance 4) */
structure field_extension_k_f where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* finite field 鈥? (phase 1, importance 3) */
structure finite_field where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* finite_field_f_{p^k} 鈥?Unique field of size p^k. Galois group cyclic. (phase 1, importance 4) */
structure finite_field_f_{p^k} where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* frobenius_automorphism_finite_fields 鈥?Frob_p: x鈫^p. Generates Gal(F_{p^k}/F_p) 鈮?Z/kZ. (phase 1, importance 5) */
structure frobenius_automorphism_finite_fields where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* fundamental_theorem_of_galois_theory 鈥?Bijection between intermediate fields and subgroups of Gal(E/F). (phase 1, importance 5) */
structure fundamental_theorem_of_galois_theory where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* galois group 鈥? (phase 1, importance 3) */
structure galois_group where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* galois_extension 鈥?Normal + separable extension. Key to FLT. (phase 1, importance 5) */
structure galois_extension where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* general_linear_group_gl鈧俽 鈥?Group of invertible 2脳2 matrices over R. (phase 1, importance 4) */
structure general_linear_group_gl鈧俽 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* group 鈥? (phase 1, importance 3) */
theorem group_exists_in_mathlib4 : True := by
  trivial
  -- Type: Group

/* group_action_on_sets 鈥?G脳X鈫扻 with (gh)路x=g路(h路x). Fundamental concept. (phase 1, importance 4) */
structure group_action_on_sets where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* group_representation 鈥?蟻: G鈫扜L(V), group homomorphism to linear maps. (phase 1, importance 5) */
structure group_representation where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* homomorphism 鈥? (phase 1, importance 3) */
theorem homomorphism_exists_in_mathlib4 : True := by
  trivial
  -- Type: MonoidHom

/* infinite_descent 鈥? (phase 1, importance 3) */
structure infinite_descent where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* integer 鈥? (phase 1, importance 3) */
structure integer where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* irreducible_representation 鈥?No proper non-trivial G-invariant subspaces. (phase 1, importance 5) */
structure irreducible_representation where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* isomorphism 鈥? (phase 1, importance 3) */
theorem isomorphism_exists_in_mathlib4 : True := by
  trivial
  -- Type: MulEquiv

/* kernel 鈥? (phase 1, importance 3) */
theorem kernel_exists_in_mathlib4 : True := by
  trivial
  -- Type: Subgroup.kernel

/* lemma_6_1 鈥? (phase 1, importance 5) */
structure lemma_6_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* linear_algebra 鈥? (phase 1, importance 3) */
structure linear_algebra where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* module 鈥? (phase 1, importance 3) */
theorem module_exists_in_mathlib4 : True := by
  trivial
  -- Type: Module

/* normal_extension 鈥?Irreducible polys with one root in E split completely. (phase 1, importance 4) */
theorem normal_extension_exists_in_mathlib4 : True := by
  trivial
  -- Type: FieldTheory.Normal

/* number_theory 鈥? (phase 1, importance 3) */
structure number_theory where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* orbit 鈥?Gx = {g路x | g鈭圙}, partition of X. (phase 1, importance 3) */
structure orbit where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* polynomial 鈥? (phase 1, importance 3) */
structure polynomial where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* prime 鈥? (phase 1, importance 3) */
theorem prime_exists_in_mathlib4 : True := by
  trivial
  -- Type: Nat.Prime

/* proposition_10_1 鈥? (phase 1, importance 5) */
structure proposition_10_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_6_1 鈥? (phase 1, importance 5) */
structure proposition_6_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* quotient 鈥? (phase 1, importance 3) */
theorem quotient_exists_in_mathlib4 : True := by
  trivial
  -- Type: QuotientGroup

/* quotient_set_x_鈭?鈥?Identifying equivalent elements via relation. (phase 1, importance 3) */
structure quotient_set_x_鈭?where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* quotient_space_v_w 鈥?Vector space modulo subspace. (phase 1, importance 3) */
structure quotient_space_v_w where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* rational_prime 鈥? (phase 1, importance 3) */
structure rational_prime where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* ring 鈥? (phase 1, importance 3) */
theorem ring_exists_in_mathlib4 : True := by
  trivial
  -- Type: Ring

/* root_of_unity 鈥? (phase 1, importance 3) */
structure root_of_unity where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* separable_extension 鈥?Minimal polynomials have distinct roots. (phase 1, importance 4) */
theorem separable_extension_exists_in_mathlib4 : True := by
  trivial
  -- Type: FieldTheory.Separable

/* special_linear_group_sl鈧俽 鈥?2脳2 matrices with determinant 1. Key to modular forms. (phase 1, importance 5) */
structure special_linear_group_sl鈧俽 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* stabiliser_subgroup 鈥?G_x = {g鈭圙 | g路x=x}, subgroup of G. (phase 1, importance 3) */
structure stabiliser_subgroup where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_10_1 鈥? (phase 1, importance 5) */
structure theorem_10_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_10_2 鈥? (phase 1, importance 5) */
structure theorem_10_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_8_1 鈥? (phase 1, importance 5) */
structure theorem_8_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_8_2 鈥? (phase 1, importance 5) */
structure theorem_8_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_corollary_7_1 鈥? (phase 1, importance 3) */
structure thm_corollary_7_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_corollary_9_1 鈥? (phase 1, importance 3) */
structure thm_corollary_9_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_11_10 鈥? (phase 1, importance 3) */
structure thm_lemma_11_10 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_11_12 鈥? (phase 1, importance 3) */
structure thm_lemma_11_12 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_11_2 鈥? (phase 1, importance 3) */
structure thm_lemma_11_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_11_4 鈥? (phase 1, importance 3) */
structure thm_lemma_11_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_11_7 鈥? (phase 1, importance 3) */
structure thm_lemma_11_7 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_11_9 鈥? (phase 1, importance 3) */
structure thm_lemma_11_9 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_14_1 鈥? (phase 1, importance 3) */
structure thm_lemma_14_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_6_1 鈥? (phase 1, importance 3) */
structure thm_lemma_6_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_7_3 鈥? (phase 1, importance 3) */
structure thm_lemma_7_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_7_4 鈥? (phase 1, importance 3) */
structure thm_lemma_7_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_9_11 鈥? (phase 1, importance 3) */
structure thm_lemma_9_11 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_9_12 鈥? (phase 1, importance 3) */
structure thm_lemma_9_12 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_9_5 鈥? (phase 1, importance 3) */
structure thm_lemma_9_5 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_9_6 鈥? (phase 1, importance 3) */
structure thm_lemma_9_6 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_lemma_9_9 鈥? (phase 1, importance 3) */
structure thm_lemma_9_9 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_10_1 鈥? (phase 1, importance 3) */
structure thm_proposition_10_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_11_1 鈥? (phase 1, importance 3) */
structure thm_proposition_11_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_11_2 鈥? (phase 1, importance 3) */
structure thm_proposition_11_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_11_6 鈥? (phase 1, importance 3) */
structure thm_proposition_11_6 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_11_7 鈥? (phase 1, importance 3) */
structure thm_proposition_11_7 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_14_1 鈥? (phase 1, importance 3) */
structure thm_proposition_14_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_14_2 鈥? (phase 1, importance 3) */
structure thm_proposition_14_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_6_1 鈥? (phase 1, importance 3) */
structure thm_proposition_6_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_7_2 鈥? (phase 1, importance 3) */
structure thm_proposition_7_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_7_3 鈥? (phase 1, importance 3) */
structure thm_proposition_7_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_7_4 鈥? (phase 1, importance 3) */
structure thm_proposition_7_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_7_5 鈥? (phase 1, importance 3) */
structure thm_proposition_7_5 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_7_6 鈥? (phase 1, importance 3) */
structure thm_proposition_7_6 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_9_1 鈥? (phase 1, importance 3) */
structure thm_proposition_9_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_10_1 鈥? (phase 1, importance 3) */
structure thm_theorem_10_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_10_2 鈥? (phase 1, importance 3) */
structure thm_theorem_10_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_11_1 鈥? (phase 1, importance 3) */
structure thm_theorem_11_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_11_11 鈥? (phase 1, importance 3) */
structure thm_theorem_11_11 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_11_13 鈥? (phase 1, importance 3) */
structure thm_theorem_11_13 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_11_2 鈥? (phase 1, importance 3) */
structure thm_theorem_11_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_11_4 鈥? (phase 1, importance 3) */
structure thm_theorem_11_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_13_1 鈥? (phase 1, importance 3) */
structure thm_theorem_13_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_14_3 鈥? (phase 1, importance 3) */
structure thm_theorem_14_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_7_3 鈥? (phase 1, importance 3) */
structure thm_theorem_7_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_8_2 鈥? (phase 1, importance 3) */
structure thm_theorem_8_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_9_2 鈥? (phase 1, importance 3) */
structure thm_theorem_9_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_9_8 鈥? (phase 1, importance 3) */
structure thm_theorem_9_8 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* trace_of_matrix 鈥?Tr(X)=a+d, sum of diagonal. Invariant under conjugacy. (phase 1, importance 3) */
structure trace_of_matrix where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* unity_algebraic 鈥? (phase 1, importance 3) */
structure unity_algebraic where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* vector space 鈥? (phase 1, importance 3) */
structure vector_space where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* witt_vector 鈥? (phase 1, importance medium) */
structure witt_vector where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

end FLT

