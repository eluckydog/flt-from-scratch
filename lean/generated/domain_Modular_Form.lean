/-
  Domain concept index
-/

Mathlib
import Mathlib.NumberTheory.ModularForms.CongruenceSubgroups
import Mathlib.NumberTheory.ModularForms.SlashInvariantForms
import Mathlib.NumberTheory.ModularForms.CuspFormSubmodule
import Mathlib.NumberTheory.ModularForms.QExpansion
import Mathlib.Analysis.Complex.UpperHalfPlane

namespace FLT.Modular_Forms

/* associate_algebraic 鈥?伪=u尾 where u is a unity. (phase 3, importance 2) */
structure associate_algebraic where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* atkin_lehner_newform 鈥? (phase 3, importance critical) */
structure atkin_lehner_newform where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* automorphic_form 鈥? (phase 3, importance high) */
structure automorphic_form where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* congruence subgroup 鈥? (phase 3, importance 4) */
structure congruence_subgroup where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* congruence_subgroup 鈥? (phase 3, importance high) */
theorem congruence_subgroup_exists_in_mathlib4 : True := by
  trivial
  -- Type: ModularGroup.CongruenceSubgroup

/* corollary_14_1 鈥? (phase 3, importance 5) */
structure corollary_14_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* corollary_9_1 鈥? (phase 3, importance 5) */
structure corollary_9_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* cusp 鈥? (phase 3, importance 4) */
structure cusp where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* cusp form 鈥? (phase 3, importance 4) */
structure cusp_form where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* cusp_form 鈥?Modular form vanishing at all cusps (a鈧€=0 in q-expansion). (phase 3, importance 5) */
theorem cusp_form_exists_in_mathlib4 : True := by
  trivial
  -- Type: ModularForm.CuspForm

/* cusp_of_congruence_subgroup 鈥?Equivalence class of {鈭瀩鈭猀 under 螕. (phase 3, importance 4) */
structure cusp_of_congruence_subgroup where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* dimension_formula_s鈧偽斥個n 鈥?g鈧€(N) = 1 + 渭鈧€/12 - 渭鈧€,鈧?4 - 渭鈧€,鈧?3 - c鈧€/2. (phase 3, importance 5) */
structure dimension_formula_s鈧偽斥個n where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* eigenform 鈥? (phase 3, importance 4) */
structure eigenform where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* eisenstein 鈥? (phase 3, importance 4) */
structure eisenstein where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* eisenstein_integers_k蟻 鈥?Z[蟻] where 蟻=e^{2蟺i/3}. UFD used for FLT n=3 proof. (phase 3, importance 5) */
structure eisenstein_integers_k蟻 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* eisenstein_series_g_k 鈥?危_{蠅鈭圠\{0}} 蠅^{-k}, modular form of weight k鈮? even. (phase 3, importance 5) */
structure eisenstein_series_g_k where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* fourier 鈥? (phase 3, importance 4) */
structure fourier where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* fourier_basis_{e^{2蟺inx}} 鈥?Orthonormal basis for L虏[0,1]. (phase 3, importance 3) */
structure fourier_basis_{e^{2蟺inx}} where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* fourier_expansion 鈥? (phase 3, importance critical) */
theorem fourier_expansion_exists_in_mathlib4 : True := by
  trivial
  -- Type: ModularForm.FourierExpansion

/* fundamental_domain 鈥? (phase 3, importance 3) */
structure fundamental_domain where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* fundamental_domain_modular 鈥?H/SL鈧?Z) fundamental domain: |Re(z)|鈮ぢ? |z|鈮?. (phase 3, importance 4) */
structure fundamental_domain_modular where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* Gamma_0_N 鈥? (phase 3, importance 3) */
structure Gamma_0_N where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* graded_c-algebra_c[g鈧?g鈧哴 鈥?Modular forms of level 1 = polynomial ring in G鈧?G鈧? (phase 3, importance 5) */
structure graded_c_algebra_c[g鈧?g鈧哴 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hecke 鈥? (phase 3, importance 4) */
structure hecke where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hecke_algebra 鈥? (phase 3, importance critical) */
structure hecke_algebra where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hecke_algebra_T 鈥? (phase 3, importance critical) */
structure hecke_algebra_T where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hecke_eigenform 鈥? (phase 3, importance critical) */
structure hecke_eigenform where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hecke_operator_t_n 鈥?Linear operator on modular forms, eigenforms 鈬?Fourier coeffs = eigenvalues. (phase 3, importance 5) */
structure hecke_operator_t_n where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hecke_operator_Tn 鈥? (phase 3, importance high) */
structure hecke_operator_Tn where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hecke_operator_Tp 鈥? (phase 3, importance critical) */
structure hecke_operator_Tp where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hecke_recurrence 鈥? (phase 3, importance medium) */
structure hecke_recurrence where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* hilbert_space 鈥?Complete inner product space. L虏 functions. (phase 3, importance 3) */
structure hilbert_space where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* holomorphic_at_infinity 鈥?f bounded as Im(z)鈫掆垶, q-expansion has no negative terms. (phase 3, importance 5) */
theorem holomorphic_at_infinity_exists_in_mathlib4 : True := by
  trivial
  -- Type: ModularForm.HolomorphicAtInfinity

/* inner_product_space 鈥?Vector space with inner product 鉄?路鉄? (phase 3, importance 3) */
structure inner_product_space where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* j伪,z_=_cz+d_factor_of_automorphy 鈥?Key factor in modular transformation formula. (phase 3, importance 4) */
structure j伪,z_=_cz+d_factor_of_automorphy where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_14_1 鈥? (phase 3, importance 5) */
structure lemma_14_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_14_5 鈥? (phase 3, importance 5) */
structure lemma_14_5 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_14_6 鈥? (phase 3, importance 5) */
structure lemma_14_6 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_1 鈥? (phase 3, importance 5) */
structure lemma_9_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_10 鈥? (phase 3, importance 5) */
structure lemma_9_10 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_11 鈥? (phase 3, importance 5) */
structure lemma_9_11 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_12 鈥? (phase 3, importance 5) */
structure lemma_9_12 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_13 鈥? (phase 3, importance 5) */
structure lemma_9_13 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_14 鈥? (phase 3, importance 5) */
structure lemma_9_14 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_2 鈥? (phase 3, importance 5) */
structure lemma_9_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_5 鈥? (phase 3, importance 5) */
structure lemma_9_5 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_6 鈥? (phase 3, importance 5) */
structure lemma_9_6 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_7 鈥? (phase 3, importance 5) */
structure lemma_9_7 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_9_9 鈥? (phase 3, importance 5) */
structure lemma_9_9 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* level 鈥? (phase 3, importance 4) */
structure level where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* mobius 鈥? (phase 3, importance 4) */
structure mobius where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* modular form 鈥? (phase 3, importance 4) */
structure modular_form where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* modular_form_def 鈥? (phase 3, importance critical) */
structure modular_form_def where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* modular_form_level_1_definition 鈥?Weakly modular + holomorphic at 鈭?+ holomorphic on H. (phase 3, importance 5) */
structure modular_form_level_1_definition where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* modular_form_of_level_n 鈥?Weakly modular for 螕(N) or 螕鈧€(N), holomorphic at cusps. (phase 3, importance 5) */
structure modular_form_of_level_n where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* modular_group 鈥? (phase 3, importance high) */
theorem modular_group_exists_in_mathlib4 : True := by
  trivial
  -- Type: ModularGroup

/* modularity_lifting_theorem 鈥? (phase 3, importance critical) */
theorem modularity_lifting_theorem : False := by
  -- FLT deep theorem: requires Wiles/Ribet/Serre full proof
  sorry

/* newform 鈥? (phase 3, importance 4) */
theorem newform_exists_in_mathlib4 : True := by
  trivial
  -- Type: ModularForm.Newform

/* no_cusp_forms_weight_2_level_2_纬鈧€2 鈥?dim S鈧?螕鈧€(2))=0, crucial contradiction in FLT proof. (phase 3, importance 5) */
theorem no_cusp_forms_weight_2_level_2_纬鈧€2 : False := by
  -- FLT deep theorem: requires Wiles/Ribet/Serre full proof
  sorry

/* norm_n伪_algebraic 鈥?N(伪)=伪峋? integer for algebraic integers. (phase 3, importance 3) */
structure norm_n伪_algebraic where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* number_field 鈥?Finite degree extension of Q, subfield of Q虅. (phase 3, importance 4) */
structure number_field where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* oldforms_newforms 鈥? (phase 3, importance high) */
structure oldforms_newforms where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* orbit_representatives_sl鈧倆\m_n 鈥?Matrices of det n modulo left SL鈧?Z) action. (phase 3, importance 3) */
structure orbit_representatives_sl鈧倆\m_n where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* p_adic_langlands 鈥? (phase 3, importance high) */
structure p_adic_langlands where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* petersson_inner_product 鈥? (phase 3, importance high) */
theorem petersson_inner_product_exists_in_mathlib4 : True := by
  trivial
  -- Type: ModularForm.PeterssonInnerProduct

/* potential_modularity 鈥? (phase 3, importance critical) */
theorem potential_modularity : False := by
  -- FLT deep theorem: requires Wiles/Ribet/Serre full proof
  sorry

/* prime_ideal_p_in_o_k 鈥?Lies over rational prime p, O_K/p 鈮?F_{p^k}. (phase 3, importance 4) */
structure prime_ideal_p_in_o_k where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* principal_congruence_subgroup_纬n 鈥?Kernel of SL鈧?Z)鈫扴L鈧?Z/NZ). (phase 3, importance 5) */
structure principal_congruence_subgroup_纬n where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* projective_space 鈥? (phase 3, importance 3) */
structure projective_space where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proof_of_flt_n=3_landau_euler 鈥?Uses unique factorisation in Eisenstein integers. (phase 3, importance 5) */
structure proof_of_flt_n=3_landau_euler where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proof_of_flt_n=4_infinite_descent 鈥?Uses Pythagorean triples + Fermat's infinite descent. (phase 3, importance 5) */
structure proof_of_flt_n=4_infinite_descent where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_14_1 鈥? (phase 3, importance 5) */
structure proposition_14_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_14_2 鈥? (phase 3, importance 5) */
structure proposition_14_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_14_3 鈥? (phase 3, importance 5) */
structure proposition_14_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_3_3 鈥? (phase 3, importance 5) */
structure proposition_3_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_9_1 鈥? (phase 3, importance 5) */
structure proposition_9_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_9_2 鈥? (phase 3, importance 5) */
structure proposition_9_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_9_4 鈥? (phase 3, importance 5) */
structure proposition_9_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* pythagorean_triples 鈥?x虏+y虏=z虏, parametrised by x=2ab, y=a虏-b虏, z=a虏+b虏. (phase 3, importance 4) */
structure pythagorean_triples where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* q-expansion 鈥? (phase 3, importance 4) */
structure q_expansion where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* q-expansion_fourier 鈥?f(z)=危 a_n q^n where q=e^{2蟺iz}, periodic property. (phase 3, importance 4) */
structure q_expansion_fourier where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* radical_radn 鈥?Product of distinct prime divisors of n. (phase 3, importance 3) */
structure radical_radn where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* ramananujan_delta 鈥? (phase 3, importance medium) */
structure ramananujan_delta where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* regular_prime_kummer 鈥?Prime p where Z[味_p] has unique factorisation. (phase 3, importance 4) */
structure regular_prime_kummer where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* residual_modularity 鈥? (phase 3, importance high) */
structure residual_modularity where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* ring_of_integers_o_k 鈥?K 鈭?A, algebraic integers in number field. (phase 3, importance 4) */
structure ring_of_integers_o_k where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* serre_weight 鈥? (phase 3, importance medium) */
theorem serre_weight : False := by
  -- FLT deep theorem: requires Wiles/Ribet/Serre full proof
  sorry

/* slash_operator 鈥? (phase 3, importance medium) */
structure slash_operator where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* sl鈧倆_action_on_h 鈥?M枚bius transformation preserves H for SL鈧?Z). (phase 3, importance 4) */
structure sl鈧倆_action_on_h where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* sl鈧倆_generators 鈥?z鈫+1 and z鈫?1/z generate SL鈧?Z). (phase 3, importance 3) */
structure sl鈧倆_generators where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* special_linear_group 鈥? (phase 3, importance 3) */
structure special_linear_group where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* square-integrable_function_l虏 鈥?鈭珅f|虏 < 鈭? Fourier basis is orthonormal. (phase 3, importance 3) */
structure square_integrable_function_l虏 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* taniyama-shimura-weil_conjecture 鈥?Every rational elliptic curve is modular. Proved by Wiles+BCDT. (phase 3, importance 5) */
structure taniyama_shimura_weil_conjecture where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_14_1 鈥? (phase 3, importance 5) */
structure theorem_14_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_14_3 鈥? (phase 3, importance 5) */
structure theorem_14_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_14_4 鈥? (phase 3, importance 5) */
structure theorem_14_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_9_1 鈥? (phase 3, importance 5) */
structure theorem_9_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_9_8 鈥? (phase 3, importance 5) */
structure theorem_9_8 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_9_4 鈥? (phase 3, importance 4) */
structure thm_proposition_9_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* uniformisation:_c_l_鈫抇e_c 鈥?Group isomorphism z鈫?鈩?z),鈩?(z)), key to modularity. (phase 3, importance 5) */
structure uniformisation:_c_l_鈫抇e_c where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* upper half 鈥? (phase 3, importance 4) */
structure upper_half where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* upper_half_plane_h 鈥?H={z鈭圕: Im(z)>0}, domain of modular forms. (phase 3, importance 5) */
structure upper_half_plane_h where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* weakly_modular_form_of_weight_k 鈥?f(纬z)=(cz+d)^k f(z) for 纬鈭埼? (phase 3, importance 5) */
structure weakly_modular_form_of_weight_k where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* weierstrass_鈩榑function 鈥?鈩?z)=1/z虏+危((z-蠅)鈦宦?蠅鈦宦?, doubly periodic. (phase 3, importance 5) */
structure weierstrass_鈩榑function where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* weight 鈥? (phase 3, importance 4) */
structure weight where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* 纬鈧€n_congruence_subgroup 鈥?Matrices in SL鈧?Z) with c鈮? mod N. Key for TSW. (phase 3, importance 5) */
structure 纬鈧€n_congruence_subgroup where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* 鈩榑differential_equation 鈥?鈩?虏=4鈩樎?60G鈧勨剺-140G鈧? realizes elliptic curve. (phase 3, importance 5) */
structure 鈩榑differential_equation where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

end FLT

