/-
  Domain concept index
-/

Mathlib
import Mathlib.AlgebraicGeometry.EllipticCurve.Weierstrass
import Mathlib.AlgebraicGeometry.EllipticCurve.Affine
import Mathlib.AlgebraicGeometry.EllipticCurve.Group

namespace FLT.Elliptic_Curves

/* additive_reduction 鈥? (phase 2, importance 3) */
theorem additive_reduction_exists_in_mathlib4 : True := by
  trivial
  -- Type: EllipticCurve.Reduction.Additive

/* bad reduction 鈥? (phase 2, importance 3) */
structure bad_reduction where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* bad_reduction 鈥? (phase 2, importance 3) */
theorem bad_reduction_exists_in_mathlib4 : True := by
  trivial
  -- Type: EllipticCurve.Reduction.Bad

/* cayley-bacharach_theorem 鈥?Cubic through 8 of 9 intersection points passes through 9th. (phase 2, importance 3) */
structure cayley_bacharach_theorem where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* cayley_bacharach 鈥? (phase 2, importance 3) */
structure cayley_bacharach where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* conductor 鈥? (phase 2, importance 3) */
theorem conductor_exists_in_mathlib4 : True := by
  trivial
  -- Type: EllipticCurve.Conductor

/* conductor_n_e_general 鈥?N_E = 螤 p^{f_p}, f_p measures ramification. (phase 2, importance 5) */
structure conductor_n_e_general where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* corollary_7_1 鈥? (phase 2, importance 5) */
structure corollary_7_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* cusp_singularity 鈥? (phase 2, importance 3) */
structure cusp_singularity where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* discriminant 鈥? (phase 2, importance 3) */
theorem discriminant_exists_in_mathlib4 : True := by
  trivial
  -- Type: EllipticCurve.Weierstrass.discriminant

/* e[n]_鈮卂z_nz鈯晍_nz 鈥?n-torsion is direct sum of two cyclic groups. (phase 2, importance 5) */
structure e[n]_鈮卂z_nz鈯晍_nz where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* elliptic curve 鈥? (phase 2, importance 3) */
structure elliptic_curve where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* elliptic_curve_group_law 鈥?Geometric chord-tangent addition on elliptic curve. (phase 2, importance 5) */
structure elliptic_curve_group_law where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* frey 鈥? (phase 2, importance 3) */
structure frey where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* frey curve 鈥? (phase 2, importance 3) */
structure frey_curve where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* good reduction 鈥? (phase 2, importance 3) */
structure good_reduction where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* good_reduction 鈥? (phase 2, importance 3) */
theorem good_reduction_exists_in_mathlib4 : True := by
  trivial
  -- Type: EllipticCurve.Reduction.Good

/* inverse_limit 鈥?General construction: lim A_i with compatible maps. (phase 2, importance 3) */
structure inverse_limit where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* inverse_system 鈥?System of groups/homomorphisms for inverse limit. (phase 2, importance 3) */
structure inverse_system where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* j-invariant 鈥? (phase 2, importance 3) */
structure j_invariant where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lattice 鈥? (phase 2, importance 3) */
structure lattice where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lattice_l 鈥?Discrete subgroup Z蠅鈧佲姇Z蠅鈧?in C. (phase 2, importance 4) */
structure lattice_l where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_3_1 鈥? (phase 2, importance 5) */
structure lemma_3_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_7_3 鈥? (phase 2, importance 5) */
structure lemma_7_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* lemma_7_4 鈥? (phase 2, importance 5) */
structure lemma_7_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* mordell 鈥? (phase 2, importance 3) */
structure mordell where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* multiplicative_reduction 鈥? (phase 2, importance 3) */
theorem multiplicative_reduction_exists_in_mathlib4 : True := by
  trivial
  -- Type: EllipticCurve.Reduction.Multiplicative

/* node_singularity 鈥? (phase 2, importance 3) */
structure node_singularity where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* p-division_points_e[p] 鈥?F_p-vector space of p-torsion points. (phase 2, importance 4) */
structure p_division_points_e[p] where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* point at infinity 鈥? (phase 2, importance 3) */
structure point_at_infinity where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* point_at_infinity 鈥? (phase 2, importance 3) */
structure point_at_infinity where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* point_at_infinity_o 鈥?Identity element of elliptic curve group. (phase 2, importance 5) */
structure point_at_infinity_o where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* pp_group_scheme 鈥? (phase 2, importance medium) */
structure pp_group_scheme where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_13_1 鈥? (phase 2, importance 5) */
structure proposition_13_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_7_1 鈥? (phase 2, importance 5) */
structure proposition_7_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_7_2 鈥? (phase 2, importance 5) */
structure proposition_7_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_7_3 鈥? (phase 2, importance 5) */
structure proposition_7_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_7_4 鈥? (phase 2, importance 5) */
structure proposition_7_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_7_5 鈥? (phase 2, importance 5) */
structure proposition_7_5 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* proposition_7_6 鈥? (phase 2, importance 5) */
structure proposition_7_6 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* rational point 鈥? (phase 2, importance 3) */
structure rational_point where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* reduction 鈥? (phase 2, importance 3) */
structure reduction where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* semistable 鈥? (phase 2, importance 3) */
structure semistable where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* semistable_elliptic_curve 鈥?All bad reduction is multiplicative. Key: Frey curve is semistable. (phase 2, importance 5) */
structure semistable_elliptic_curve where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* singular 鈥? (phase 2, importance 3) */
structure singular where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* singular_point 鈥? (phase 2, importance 3) */
structure singular_point where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* tate_鈩揺_鈮卂z_鈩揰鈯昣z_鈩?鈥?Tate module is rank 2 free Z_鈩?module. (phase 2, importance 4) */
structure tate_鈩揺_鈮卂z_鈩揰鈯昣z_鈩?where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_13_1 鈥? (phase 2, importance 5) */
structure theorem_13_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_14_2 鈥? (phase 2, importance 5) */
structure theorem_14_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_7_1 鈥? (phase 2, importance 5) */
structure theorem_7_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_7_2 鈥? (phase 2, importance 5) */
structure theorem_7_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_7_3 鈥? (phase 2, importance 5) */
structure theorem_7_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_9_2 鈥? (phase 2, importance 5) */
structure theorem_9_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* theorem_9_4 鈥? (phase 2, importance 5) */
structure theorem_9_4 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_3_1 鈥? (phase 2, importance 3) */
structure thm_proposition_3_1 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_3_2 鈥? (phase 2, importance 3) */
structure thm_proposition_3_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_proposition_3_3 鈥? (phase 2, importance 3) */
structure thm_proposition_3_3 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_3_2 鈥? (phase 2, importance 3) */
structure thm_theorem_3_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* thm_theorem_7_2 鈥? (phase 2, importance 3) */
structure thm_theorem_7_2 where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* torsion 鈥? (phase 2, importance 3) */
structure torsion where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* torsion_points 鈥?Points P on E s.t. nP=O. Key to Galois representations. (phase 2, importance 5) */
theorem torsion_points_exists_in_mathlib4 : True := by
  trivial
  -- Type: EllipticCurve.Torsion

/* two-dimensional_f_l_vector_space 鈥?E[l] as 2-dim vector space over F_l. (phase 2, importance 4) */
structure two_dimensional_f_l_vector_space where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* uniformisation 鈥? (phase 2, importance 3) */
structure uniformisation where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* uniformisation_theorem 鈥?Every elliptic curve over C is C/L for some lattice L. (phase 2, importance 5) */
structure uniformisation_theorem where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* weierstrass 鈥? (phase 2, importance 3) */
structure weierstrass where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* 鈩?adic_integers_z_鈩?鈥?Inverse limit of Z/鈩揯n Z. (phase 2, importance 4) */
structure 鈩揰adic_integers_z_鈩?where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

/* 鈩?adic_tate_module 鈥?Inverse limit of 鈩揯n torsion points, Z_鈩?module rank 2. (phase 2, importance 5) */
structure 鈩揰adic_tate_module where
  -- needs manual mapping to mathlib4
  -- notes: manual mapping required

end FLT

